#include <stdio.h>
int main()
{
    // array of character pointers or 
    // array of c character array or
    // array of strings
    
    char *char_array[]={"hello","what","are","you","doing"};
    // I don't understand what pointer to pinter is in C and IDC rn. for now I wanna program with pointers
    
    printf("%s", *char_array);
    return 0;
}
