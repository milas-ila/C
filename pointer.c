#include <stdio.h>

int main()
{
    // using strings with pointers
    char* string="hello how are you doing";
    //for (char* c=string; *c!=0; c++) printf("%c", *c);
    char c;
    scanf("%c", &c);
    printf("%c", c);
    return 0;
}
